import { Dimensions } from "react-native";
const { width, height } = Dimensions.get("window");
const constant = {
  headerHeight: 90,
  width: width,
  height: height,
  popupInfo: 100
};

export default constant;
